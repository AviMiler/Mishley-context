# Agent Context
_Last updated: 2026-07-27 after adding a per-file dependency manager to the code-project tree (feature 6 of the 9-feature batch, scope changed at user's request — per-file screen reached from that file's own menu, not a project-wide table)_

## What This Project Is
Mishley Code Context Bank is a Chrome MV3 extension: a floating sidebar injected into chat sites (Gemini today, an "internal chat" site configurable via `config.js`) that lets the user manage reusable context blocks, a "General Memory" auto-injected preamble, saved conversation history, and projects that can hold fixed text blocks, full documents (files/text/URLs), or an entire local code folder — all injected into the chat on demand. Context management now lives in one place (the Context tab); the History tab is conversations-only.

## Current State
- ✅ Working: sidebar mount/toggle, context blocks, General Memory auto-inject, conversation auto-save + continue flow, prompts editor, context-window meter (incl. per-file token breakdown), project documents (upload/paste/estimate/inject), code-project folder scanning + static dependency graph + file-tree picker. **New:** code projects can now maintain a manual ignore-list (`project.ignorePatterns`) of file/folder names or `*`-globs excluded from scanning, edited via a dialog opened from `#codeProjectIgnoreBtn` next to the documents header's refresh button.
- 🚧 WIP: **Real BPE token counting + the live token-budget bar (2026-07-26), plus a scan-speed fix and an inject.js hang fix (both 2026-07-27)** are implemented and Node-verified (427 tokenizer assertions vs `gpt-tokenizer`, 18 fallback-ladder assertions, 17 budget-bar assertions, 14 inject.js assertions) but **not yet browser-verified** — needs an unpacked reload to confirm the `web_accessible_resources` fetch works under a real page's CSP, that meter/file-tree numbers shift when the ranks resolve, that scanning is back to its pre-tokenizer speed, that the budget bar tracks live, and that loading files then prompts/messages no longer hangs. **Dependency counts in the deps menu + folder-aware search (2026-07-26, scope changed)** are also implemented and Node-verified (18 assertions) but not yet browser-verified. **Read-only file preview (2026-07-27, scope changed — full-pane takeover instead of a dialog overlay)** is implemented and Node-verified (24 assertions) but not yet browser-verified — see Next Steps. **The per-file dependency manager (2026-07-27, feature 6 of the batch, scope changed at the user's request)** is implemented and statically verified (a dedicated `verify-agent` pass, not a Node harness — see the Last Changes entry) but **not yet browser-verified**. These are items 1–6 of a 9-feature batch; the remaining 3 (undo injection, editable quick commands, onboarding guide) are planned but not started — the full original-design implementation is preserved on branch `feature/nine-panel-features` for cherry-picking (note: the deps-badge portion of that branch's feature 3–4 commit, the dialog-overlay portion of its file-preview commit, and the project-wide-table portion of its dependency-management-screen commit, were **not** reused as-is, since all three designs changed at the user's request); plan file at `C:\Users\User\.claude\plans\peaceful-roaming-hickey.md`. **CSS `@import` dependency-graph support (2026-07-26, same branch)** is implemented and Node-verified (7 assertions over the real module) but **not yet browser-verified** — needs a code project containing `.css` files with real `@import`s, rescanned, then "load with dependencies" on one checked in the file tree. **tree-sitter AST symbol extraction (2026-07-23, branch `feature/tree-sitter-symbols`)** is implemented and Node-verified (38 assertions over the real modules) but **not yet browser-verified** — needs an unpacked-extension reload (new manifest keys: `background`, CSP) and a rescan of a real code project checking `csAst`/`jsAst` > 0 in `[ccb-timing] dep-graph.buildGraph`. **The per-message auto-inject mode**, now split into independent GM/project settings (2026-07-22), is implemented and syntax-checked but **not yet browser-verified** (send interception on both sites, capture stripping, badge/select round-trip, and the new mixed-mode combinations — see Next Steps). **The code-project scan performance rework** (2026-07-19) is implemented, harness-verified, and documented, but not yet exercised through the actual extension on the affected machine's real large project. The ignore-list feature below is implemented and documented but **not yet manually verified end-to-end in the browser** (no unpacked-extension test yet), and not yet committed. Earlier items still pending their own browser verification: the prior session's fixes/reverts, and the Context-tab redesign.
- ❌ Known Issues: dependency-graph resolution is best-effort only (dynamic dispatch, computed member access, reflection, `eval` can't be resolved statically — see [dep-graph.js](dep-graph.js) header comment); legacy binary `.doc` files are not text-extractable. **Known-and-deliberately-skipped** (user decision 2026-07-21, stack is React-without-TS + .NET): the dep graph does not do TS NodeNext `.js`→`.ts` mapping, does not analyze Vue/Svelte SFC imports, does not support tsconfig `paths`/`baseUrl` aliases beyond `@/`/`~/`, and silently no-ops (empty deps, "לא זוהו קבצים נוספים") for languages it doesn't analyze (Python/Java/Go/...).

## Last Changes
- 2026-07-27 (per-file dependency manager — feature 6 of the batch, **scope changed mid-build at the user's request**) — First attempt built a project-wide table screen (new module `dep-manager.js`: a sortable list of every scanned file with dependency/dependent counts, click-through to a per-file editor). The user stopped that build and redirected: each file's OWN existing "אפשרויות תלויות" menu (the dropdown already offering "תלויות"/"תלויים"/"הקשר מלא", which load a file's dependency closure into the chat) should instead gain a 4th item, "ניהול תלויות", opening a screen scoped to just that one file — no project-wide table. The first attempt was fully reverted (`git checkout --` on every touched file + deleting `dep-manager.js`) before rebuilding to the corrected design. Final implementation lives entirely inside the pre-existing `code-tree.js` module (no new file): `openDepsManager(doc)` opens a new full-pane view (`#depManagerView`, same `#conversationView`/`#filePreviewView` takeover pattern) showing that file's outgoing dependencies as removable rows plus an add-dependency text input with filtered suggestions from the project's own code files, and its incoming dependents as a read-only list (tagged "מחושב אוטומטית — לעריכה יש לגשת לקובץ המקורי", since they're derived from other files' outgoing edges). Persistence: `project.depGraphOverrides = { [path]: { added: [], removed: [] } }`, saved via a new `history-view.js#setFileDependencyOverride` — stored **separately** from `project.depGraph` (which `rescanCodeProject` overwrites wholesale every scan) specifically so a manual choice survives a rescan and keeps overriding the freshly-scanned default, per the user's explicit requirement. New `dep-graph.js#applyOverrides(graph, overrides)` merges overrides onto a graph **copy** (never mutates its input); a new `code-tree.js#effectiveGraph()` routes both the existing "load with dependencies" menu counts and the new manager through it, so a manual edit is immediately visible everywhere the graph is read, not just inside the new screen. Edits are computed as deltas against the RAW `_project.depGraph` (removing a real scanned edge records it in `removed`; removing an edge that only existed via a prior `added` override just un-adds it; symmetric for adding), so toggling an edge back and forth never accumulates stale override entries. All four full-pane views (conversation/file-preview/deps-manager, plus the pre-existing settings dialogs) close each other defensively on open and on Escape/tab-switch/panel-close, so at most one can ever be visible. Verified via a dedicated `verify-agent` static-review pass (no Node harness this time — this is pure DOM/UI logic with no non-DOM unit to test in isolation): confirmed the old attempt is fully gone, the override-delta math is symmetric in all four cases, `applyOverrides` doesn't mutate its input, the override survives `rescanCodeProject` by tracing that function line-by-line, and the mutual-exclusion closes are exhaustive. Browser pass pending — see Next Steps. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (meta line: per-group direction, RTL for Hebrew/LTR for English, per explicit user instruction) — Follow-up to the flexbox fix below: user gave a direct instruction rather than another "still broken" report — the "תווים" (chars) group should be `direction: rtl` (matching its actual language), and the "tokens" group `direction: ltr`, instead of forcing `ltr` uniformly on both. `code-tree.js`'s meta groups now carry an explicit `dir` alongside their pieces; `renderMetaLine` adds a `fp-meta-${dir}` class per group. `ui-styles.js#.fp-meta-item` keeps `unicode-bidi: isolate` but no longer hardcodes `direction:ltr` — two modifiers (`.fp-meta-rtl`/`.fp-meta-ltr`) set it per group. Extended `file-preview.mjs` to 33 assertions (confirms each group carries the correct modifier and not the other); re-ran the other 4 suites, all pass. This is the fourth pass at this one line — implemented per the user's specific direction this time, not a guess, but still awaits their own confirmation in a reloaded browser. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (fix, 3rd attempt: meta line ordering forced via flexbox, not text bidi) — User reported the CSS-isolate fix (previous entry) STILL didn't fix the number/Hebrew-word ordering, and gave the exact wanted output: "100 תווים · 350 tokens" (chars first, tokens second, number immediately before its own label). Both prior attempts (LRM, then `unicode-bidi:isolate` on a flat string) relied on the Unicode bidi *text* algorithm to position the number — something about this specific line kept producing the swap regardless. This attempt sidesteps text bidi resolution entirely: `code-tree.js#renderMetaLine` now builds each metric as a real flex box (`.fp-meta-item`, `display:inline-flex; direction:ltr`) containing separate `.fp-meta-piece` spans (number, then label) — visual order is decided by CSS box layout in DOM order, not character-level bidi reordering, the same technique used for RTL-safe counters/price-tags in real Hebrew/Arabic UIs. Also reordered the groups to chars-first-then-tokens per the user's spec (was tokens-first). Extended `file-preview.mjs` to 31 assertions (DOM order per group, chars group first); re-ran the other 4 suites, all pass. **Flagged plainly: this is the third attempt at the same bug** — flex ordering is structurally a stronger guarantee (not subject to bidi-algorithm ambiguity at all), but given the track record, don't treat this as resolved until actually confirmed in a reloaded browser. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (fix: the LRM bidi fix didn't hold up — number/Hebrew-word swap fixed with real per-segment isolation) — User reported the token/char line was STILL showing "תווים 700" instead of "700 תווים" after the previous LRM-based fix, confirming that attempt (already flagged as unverified) genuinely didn't work — a single invisible LTR mark next to the digits wasn't enough to pin the numeric run's position against the line's larger bidi context. Replaced it with the textbook-correct tool for this exact problem: `code-tree.js`'s new `renderMetaLine()` builds the meta line as real DOM nodes — each segment ("N tokens", "N תווים", truncation note) gets its own `<span class="fp-meta-item">` — and `ui-styles.js` gives that class `unicode-bidi: isolate`, a genuine bidi isolation boundary (as opposed to an embedded invisible character within one text run). Removed the now-superseded LRM append from `fmt()`. Upgraded `file-preview.mjs`'s DOM mock to properly aggregate `textContent` recursively (matching real DOM — needed to observe a node built from several `appendChild` calls) and added `document.createTextNode`; replaced the LRM-count assertion with one checking for exactly 2 `.fp-meta-item` spans. All 28 assertions + the other 4 suites pass. **Honestly flagged**: `unicode-bidi: isolate` is well-established for exactly this bug class and much more robust than the LRM attempt, but given that attempt was also expected to work, this still needs real-browser confirmation, not just trust. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (file preview header: close icon, full path below the file name, bidi fix for token/char line) — Follow-up screenshot feedback: back-chevron should be an X (close) instead; the path line should show the FULL path including the file name, below the bold file name (not above it as directory-only); and the token/char line had a real RTL bug — a number immediately followed by a Hebrew word visually swapped positions ("683 תווים" rendered as "תווים 683"). `ui-template.js#fpBack` now uses `IC.x`/`aria-label="סגור"`; header order flipped to title-then-path. `ui-styles.js` scoped the chevron-flip rotation back to `#cvBack` only. `code-tree.js#openPreview` sets `fpPath` to the full `doc.name`. Bidi fix: appended an invisible LRM (U+200E) after every formatted number in `fmt()` — anchors the digits as a strong LTR run so they can't be pulled toward the following Hebrew word, a standard technique for this exact bug class, **not yet confirmed in a real browser** (Node can assert the mark is present, not that rendering is actually fixed). Extended `file-preview.mjs` to 28 assertions; re-ran the other 4 suites, all pass. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (fix: file preview header was unstyled; file name shown separately from its path) — User reported the file preview's back button looked unstyled and the header area was messy, and asked for the bare file name to show separately, emphasized, apart from the full path. Root cause: `#filePreviewView` was built with new ids (`#fpBack`/`#fpTitle`) while the reused conversation-preview CSS targets the OLD ids (`#cvBack`/`#cvTitle`) specifically — those rules never matched the new elements at all (only the *class*-based shared rules did), so the back button rendered bare and the title had no bold/ellipsis styling. Fixed by extending `#cvBack`'s rules to also match `#fpBack`. Separately, `.cv-meta`'s `direction: rtl` was scrambling the mixed-language meta line ("X tokens · Y תווים") — gave it its own LTR `.fp-meta` class instead. Added a new `#fpPath` line (small, dim, directory portion only) above the now-bold-and-centered `#fpTitle` (bare file name only) — `code-tree.js#openPreview` splits `doc.name` on `/` to produce both. Extended `file-preview.mjs` to 27 assertions (nested + root-level file path-splitting, on top of everything previously verified); re-ran the other 4 suites, all pass. This is a CSS/visual fix a Node harness can assert *content* for but not actually render — browser pass still pending. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (fix: file-row action buttons still left too much space before the tree border) — Follow-up to the button regrouping right below: user pointed out (with a screenshot) a visible empty strip still remained between the icon cluster and the tree's left border. Root cause: `.code-tree-file-actions`'s `margin-inline-end: -4px` only canceled the row's own 4px padding, not the separate 6px padding on the containing `.code-tree-body`. Changed to `-10px` (4+6) so the cluster sits fully flush against the border. Pure CSS change; re-ran `file-preview.mjs`/`deps-menu.mjs`, both still pass. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (file-row action buttons: bigger, tighter, more room for the filename) — User feedback on the preview/deps buttons: icons a bit bigger, closer together, and more width freed up for the filename. `code-tree.js` now wraps both buttons in `.code-tree-file-actions` (named distinctly from the pre-existing `.code-tree-actions` toolbar above the tree, to avoid a collision) with its own `gap: 3px` and a `margin-inline-end: -4px` that cancels the row's edge padding, flush to the row's far edge — reclaiming that space for the label. Button size 18px→20px, icon SVGs explicitly sized to 14px via a scoped selector (the shared `IC.link`/`IC.eye` strings are untouched, since `IC.link` is also used at its original size by the deps dropdown menu). Re-ran `file-preview.mjs` (24) and `deps-menu.mjs` (18); both pass. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-27 (read-only file preview from the code-project tree — feature 5 of the batch, **scope changed at the user's request**) — The original plan called for a small `ui-modals.js` dialog overlay (~720px, centered). The user explicitly asked instead for it to open across the whole chat area, the same way the History tab's conversation preview does ("אמור להיפתח על כל שטח הצ'אט כמו הצגת שיחה היסטורית") — the same category of deviation as feature 3–4's badge-to-menu scope change earlier in this batch. Built as a second full-pane takeover view, `#filePreviewView` (`ui-template.js`), sharing `#conversationView`'s exact CSS positioning/`.cv-open` toggle/`.cv-shell` structure (`ui-styles.js`'s rule became `#conversationView, #filePreviewView { ... }`, comma-joined) rather than the dialog-overlay pattern. Each file row in `code-tree.js` gained a second action button (eye icon, its own `code-tree-preview-btn` class — deliberately distinct from `code-tree-deps-btn` so the two stay unambiguously selectable) opening `openPreview(doc)`: reads content via the existing batched `docHandler.getCodeContents([id])`, measures the token count **live** via `estimateTextTokens` (never `doc.estimatedTokens`, which can carry a stale pre-tokenizer estimate), caps the displayed text at 20,000 chars (display-only — never affects injection) while the meta line always reports the true length + a truncation note when capped, and writes title/body/meta via `.textContent` only (never `.innerHTML` — file content is arbitrary user-project text). Since this view and the conversation-preview view are both fixed, same-z-index takeovers of the same screen area, each closes the other before opening (`openPreview` calls `historyView.closeConversationView()`; `history-view.js#openConversationView` calls `window.__ccbCodeTree.closeFilePreview()`), and `closeFilePreview()` is wired alongside every existing `closeConversationView()` call site in `content.js` (Escape key, tab switch, panel close) plus its own back button (`#fpBack`) so it can never linger open behind a tab switch or a closed panel. `closeFilePreview()` also clears `#fpBody`'s `textContent` on close so a large file isn't pinned in the DOM. Verified with a 24-assertion Node harness over the real `code-tree.js` (button rendering + distinct class from the deps button; live token measurement beating a stale stored estimate; truncation at 20,000 chars while the true length/count is still reported; content written verbatim via `textContent` with zero child elements even for `<script>`-looking input; the cross-close guard firing; both failure paths — missing content, thrown error — surfacing a `setStatus(..., true)` call and leaving the view closed; close clearing the body). Browser pass pending — see Next Steps. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-27 (fix: "טען קבצים" still hung — execCommand("insertText") itself was the bottleneck, not just its selection scope) — The same-day point-insertion fix (below) narrowed *what* gets read/replaced but the user reported the hang persisted. Measured directly in a real browser (Node has no DOM/`execCommand`): `document.execCommand("insertText", false, text)` on a `contenteditable` did not return within 60+ seconds for a ~300,000-char code-shaped string — completely wedged the tab. The same insert done via `Range.insertNode` on a manually built `DocumentFragment` (`inject.js#insertRangeText`/`buildLineFragment`, alternating `Text` nodes with real `<br>` elements per line) measured linear: 100K chars ~100ms, 2M ~2s, 5M ~5.3s. `replaceContentEditable` now uses the same helper (clears via `el.replaceChildren()` instead of `execCommand`). Real `<br>` elements rather than raw `"\n"` in one text node, since a `contenteditable` isn't guaranteed `white-space: pre-wrap`. Verified with 14 assertions run in an actual browser tab (exact content-equivalence for empty/prepend/append/replace, `<br>` count matches newline count, textarea path unaffected, 3M-char replace in ~1.4s) — a stronger check than the earlier Node-mock verification, since the whole point was that `execCommand`'s real-world cost can't be observed outside a real renderer. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-27 (dependency counts in the deps menu + folder-aware search — **scope changed from the original design at the user's request**) — Features 3–4 of the batch. The original design (preserved on `feature/nine-panel-features`) put a `↓N ↑M` badge on every file row, backed by a new `dep-graph.js#getAllEdgeCounts` that batches the reverse-graph build once per render. **The user explicitly asked to skip the tree-wide badge** and instead show the counts only inside the per-file "תלויות"/"תלויים"/"הקשר מלא" menu — the one whose click actually injects into the chat. Since counting now happens once per menu-open rather than once per row on every render, no batch helper was needed at all: `code-tree.js#openDepsMenu` computes each item's count directly from the **existing** `dep-graph.js` functions (`getTransitiveClosure`, `getDirectDependents`, `getFullContext`) — no `dep-graph.js` changes in this pass. `getAllEdgeCounts` is deliberately deferred to the future dependency-management screen (feature 6), which actually needs a project-wide table. Folder-aware search (`matchesQuery`) is unchanged from the original design — a folder name surfaces every file beneath it, a slash-bearing query matches path segments via `startsWith` (not `includes`, which would also match `src/myUtilsHelper.js`). One UX nuance worth keeping: when the project has no `depGraph` yet, menu items show **no parenthesized count** rather than `(0)` — `loadWithDependencies` already knows to scan on demand in that case, and `(0)` would misleadingly read as "confirmed no dependencies." Verified with an 18-assertion Node harness — the 9 original search cases; per-item counts checked against hand-computed transitive/direct/full-context sizes for both a leaf file and a heavily-depended-on one; confirmation that no badge class or per-render batch computation exists anywhere in the module; and the no-graph-yet case showing plain labels. Browser pass pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-27 (fix: "טען קבצים" still ran exact BPE per file) — User reported loading a code project's files into the chat box is very slow, on this machine specifically. Root cause: the same-day scan-speed fix (below) added `{ fast: true }` to `scanCodeProject`'s per-file estimate, but missed the **other** per-file loop with the identical shape — `history-view.js#runProjectDocumentsInjection` computes a token-count label per enabled document while assembling the injected text, and for a code project that's potentially hundreds of files, each running the exact (un-yielding, ~240× slower) BPE tokenizer synchronously on the file's full content. Fixed by passing `{ fast: true }` to that per-file label call too, matching the existing bulk-caller convention; the final injection-total estimate (shown in the success toast) stays exact by design, since that's the one single interactive count this flow surfaces. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-27 (fix: loading files then prompts/messages hung the tab) — User reported the hang; initial suspicion (the tokenizer) was wrong — [inject.js](inject.js) is untouched by any reintroduced feature, and this bug predates all of this session's work. Diagnosed live via DevTools: `document.activeElement` on the active site is a `contenteditable` DIV (the configured `CHAT_INPUT_SELECTOR` textarea doesn't match, so `findInput()`'s `document.activeElement` fallback is what actually fires). Every injection therefore went through the contenteditable path, which unconditionally read `el.innerText` (layout-dependent, forces a synchronous reflow) and did `sel.selectAllChildren(el)` + `execCommand("insertText", ...)` — select-and-replace of the **entire** field. After "טען קבצים" the field holds hundreds of KB; the next injection paid to read and rebuild all of it. Fix: `prepend`/`append` modes now insert only the new text at a collapsed caret (`document.createRange()` + `.collapse(atStart)`) instead of rebuilding the whole field; emptiness is checked via `textContent`, not `innerText`. `replace` and the empty-field case still select-all (nothing to preserve there, so no regression). Same `innerText`-is-layout-dependent bug class already documented in `ctx-meter.js`'s perf section, now found in a second file. **Honest limitation carried into the docs**: the dominant cost (forced layout + full-selection DOM replace) can't be measured by a Node harness — the fix is verified for exact content-equivalence across all mode/emptiness combinations and for no longer invoking the expensive operations on the hot path (zero `innerText` reads, zero `selectAllChildren` calls for non-empty `prepend`/`append`), not for actual browser timing. Verified: 14 new Node assertions. Browser confirmation still needed. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-27 (fix: the tokenizer made project scanning much slower) — User reported file loading got slower after the tokenizer landed. Confirmed by measurement: real BPE is **238× slower** than the char heuristic it replaced, and `scanCodeProject` calls the estimator **once per scanned file** — +3.4s on an 8.65M-char / 1,343-file corpus, extrapolating to ~+20s on a 50MB project. **The "negligible" claim in the tokenizer entry below was wrong**: that benchmark compared the tokenizer's cost against *total* scan time and never against the heuristic it replaced, which is the only comparison that answers "did this get slower". Fix: `config.js#estimateTextTokens(text, opts)` now accepts `{ fast: true }` to skip the tokenizer, `document-handler.js#estimateTokensForContent` passes it through, and the scan's per-file call uses it (3405ms → 28ms on the same corpus). Deliberately still exact: the conversation meter, `addDocument`, the injection total, and the structure doc — single interactive counts where precision is visible and the cost is one call. Justified because the heuristic's **aggregate** error over that corpus is 1.81% (per-file error is larger but the sum converges), and scan estimates only ever surface as an approximate file size in the tree. An explicit flag rather than a second local heuristic, because parallel copies of this calculation drifting apart is the exact bug that made `estimateTextTokens` canonical originally. Verified: 4 new fallback-ladder assertions (`fast:true` bypasses the tokenizer *even when loaded*, `fast:false`/no-opts stay exact, empty string safe) plus the full 427-assertion tokenizer suite and 18-assertion integration suite. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-26 (live token-budget bar) — Feature 2 of the 9-feature batch. The file tree already showed a token sum, but nothing to judge it against. Added `code-tree.js#updateBudgetBar` (driven from the existing `updateTokenCount`, so it refreshes on the same events) plus `history-view.js#appendDocsBudgetBar` for a regular project's flat document list, so both project kinds read identically. Deliberately **reuses the context meter's own CSS classes and exact severity thresholds** (>50% warn, >75% high, >90% crit) rather than defining a parallel palette — all three places in the panel that talk about context-window usage now look and behave the same. `getCtxWindow` had to be added to `__ccbCodeTree.init`'s deps in `content.js#initModules` (the module previously had no access to the context-window size at all) and to `historyView.init`'s. Edge cases handled: hides itself when the window is 0/unset instead of dividing by zero, clamps the fill at 100% so an over-budget selection can't overflow the track, and keeps the existing rule that the file *count* excludes the always-on structure doc while the token *sum* includes it (that's what the footer inject actually sends). Verified with a 17-assertion Node harness driving the real `code-tree.js` through a minimal DOM stub — threshold boundaries, clamping, disabled-doc exclusion, structure-doc rules, zero-window hiding, empty selection. Browser pass pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-26 (real BPE token counting replaces the chars→tokens heuristic) — First of a 9-feature batch the user requested after a brainstorm about making the extension feel like a more advanced AI chat and deepening the file-loading/dependency experience; the plan file lives at `C:\Users\User\.claude\plans\peaceful-roaming-hickey.md`. New [tokenizer.js](tokenizer.js) implements the o200k_base BPE merge loop + the official split regex in ~60 lines of pure JS over vendored ranks (`tokenizer/o200k_base.tiktoken`, 3.4 MB / 199,998 entries from OpenAI's public encodings endpoint — **data only, no library**, since vendoring a package would require a bundler this project deliberately lacks). `config.js#estimateTextTokens` remains the single canonical entry point every module calls; it now tries `__ccbTokenizer.countTokens` first and **falls back to the existing Hebrew-aware heuristic** whenever that returns `null` (not loaded yet / load failed / text over `MAX_EXACT_CHARS` = 500,000) — same "never break because of the new layer" principle as dep-graph's regex fallback behind tree-sitter. Three design decisions worth not re-litigating: **pure JS, not WASM** (a content script runs under the *host page's* CSP, which blocks `wasm-unsafe-eval` — the exact constraint that put tree-sitter in `background.js`; routing counts through the worker would make every count async and break `ctx-meter.js#measureMessage`'s synchronous memo); **o200k_base, not cl100k_base** (much better Hebrew tokenization, which is the whole reason the fallback heuristic has a separate Hebrew divisor; 3.4 MB vs 1.7 MB); and **inline `(?i:…)` groups expanded manually** to character-class alternations, since JS regex-modifier support is too recent to depend on. Loading is lazy and fired from `content.js#init` (which already returns early on non-active sites, so only chat-site tabs fetch the 3.4 MB); the panel renders immediately on the heuristic and sharpens when the ranks resolve (~200–400 ms). Two non-obvious follow-ons: [ctx-meter.js](ctx-meter.js) gained `resetTokenCache()`, called from the load callback, because its per-message `WeakMap` is keyed on content *length* — unchanged by a policy switch — so a visible conversation would otherwise keep heuristic numbers all session; and [document-handler.js](document-handler.js)'s markup `/1.2` discount now applies **only** on the heuristic path, since it corrected a character-counting bias the real tokenizer doesn't have (leaving it would under-count markup ~17%). Requires a `web_accessible_resources` entry — a content script's `fetch` is subject to the page origin, unlike `wasm/` which only the worker reads. Verified in Node: 427/427 exact matches vs `gpt-tokenizer`'s o200k_base encoder (400 fuzz strings over a mixed Hebrew/English/CJK/emoji/code alphabet + hand-picked edge cases + whole repo source files) and 18 fallback-ladder assertions (fetch failure, HTTP 404, module absent, over-cap input). Measured ~5.4M chars/sec. **Update (2026-07-27):** the "+1.6s, negligible" scan-cost estimate above was wrong — see the entry above this one for the measured +3.4s / ~238× regression and its fix. Browser pass pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md), [DEPENDENCIES.md](DEPENDENCIES.md).
- 2026-07-26 (CSS `@import` support in the dependency graph) — Follow-up question after explaining the tree-sitter work: the user asked which languages "load with dependencies" covers, learned HTML/CSS were leaf-only (no outgoing edges), and asked to add CSS. Scope decided: plain `.css` only (not `.scss`/`.sass`/`.less` — same `@import` syntax, could reuse this if asked for later, but out of scope now); no tree-sitter grammar involved, this is regex only, same class of feature as the pre-existing JS import resolver. Added to [dep-graph.js](dep-graph.js): `CSS_EXTENSIONS`, `CSS_IMPORT_PATTERNS` (matches `@import "x"`/`'x'`, `@import url(x)` quoted or bare, ignores a trailing media query), `findCssCandidate`/`resolveCssImport` (unlike JS, CSS `@import` has no bare-specifier/alias form — every path resolves relative to the importing file via the existing `joinRelative`; a scheme-prefixed or protocol-relative URL, e.g. a CDN stylesheet, resolves to `null` since it isn't a scanned file), and `analyzeCssFile` (wired into `buildGraph` right after the C#/Razor block). `scanRegions` gained a `lang === "css"` branch: excluded from the generic `//` line-comment detection (CSS has no `//` comments — a URL's `//` inside `url(http://...)` would otherwise start a phantom comment and blank the rest of the declaration, the same bug class already fixed for Razor) and given minimal quote tracking so a string value containing `/*` (e.g. a `content:` property) can't be misread as starting a real `/* */` comment. `[ccb-timing] dep-graph.buildGraph` gained `cssTotal`. Verified with a 7-assertion Node harness (stubbed `window`/`chrome`, real module) covering: quoted/unquoted/`url()` forms, a trailing media query, a commented-out `@import` correctly ignored, an external URL `@import` correctly unresolved, a `content: "/* ... */"` string not mistaken for a real comment, relative `../` resolution from a nested file, a JS file's `import "./x.css"` still resolving to the CSS file (pre-existing generic-resolver behavior, unaffected), and `getTransitiveClosure`/`getDirectDependents` correctly following the new CSS edges. Browser pass pending (see Next Steps). See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-23 (AST-first symbol extraction — tree-sitter WASM replaces regex for C#/JS, branch `feature/tree-sitter-symbols`) — After reviewing the repomix source (dropped into the repo folder as reference), the user asked to adopt its grammar-level parsing approach for the dep graph. Scope decided by user: C# + Razor + JS/JSX, **no TypeScript**. New `background.js` — the extension's first MV3 service worker (`"type": "module"`) — hosts vendored tree-sitter WASM (`wasm/`: web-tree-sitter 0.25.6 runtime + @vscode/tree-sitter-wasm 0.1.4 C#/JS grammars, ~6.5 MB, first third-party code in the project, see DEPENDENCIES.md). Why a worker: content scripts can't compile WASM (host page's CSP governs them); the manifest gained `content_security_policy.extension_pages` with `'wasm-unsafe-eval'`. [dep-graph.js](dep-graph.js) gained `parseViaTreeSitter` (batched `ccbTsParse` messages) + `analyzeCsharpFileFromFacts`/`analyzeJsFileFromFacts`; symbol table is per-file hybrid (AST facts when parsed, regex otherwise) and the **entire regex layer is retained as fallback** — per-file and per-language (worker dead → null → regex wholesale), so scans can't break on parser failure. `.ts`/`.tsx` stay regex; Razor markup stays regex but consumes the AST-accurate C# symbol table. Extraction in the worker is a manual cursor walk, NOT a query (queries throw on unknown node types across grammar versions). Verified in Node against the REAL modules (stubbed `chrome`, real listener): 24 fact-extraction assertions incl. both historical regex bugs, + 14 integration assertions on real `buildGraph` in AST and fallback modes — all pass; ABI pairing runtime↔grammars confirmed. `[ccb-timing] dep-graph.buildGraph` now logs `csAst/csTotal/jsAst/jsTotal`. **Browser pass pending** (see Next Steps). See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md), [DEPENDENCIES.md](DEPENDENCIES.md).
- 2026-07-22 (file-loading framing ending reworded to match the per-message style) — User asked for the "טען קבצים" file-injection ending to say "end of files, from here on is the user's request," the same pattern as the per-message General Memory/project-instructions wrapper. `config.js#FRAMING_DOCS_POST` changed from `'Reply only with "Files loaded." and wait for the first instruction.'` to `"End of the attached files. The user's actual request follows — respond to it only."` — matches `FRAMING_EVERY_POST`'s style, and is more accurate to the actual behavior: `injectProjectDocuments()` never auto-sends, so the user's real request always lands in the SAME chat message right after the files, not as a separate turn; the old wording told the model to just acknowledge while a real question followed in the same breath. `FRAMING_DOCS_PRE` untouched. `prompts.js`'s editable `docsOutro` default derives from the raw config text automatically — no prompts.js changes needed. Left `INJECTION_AUTORESPONSES`'s now-dead `"Files loaded."` entries in place (harmless; `history-view.js`'s copy explicitly exists to filter legacy saved blocks from before this change). Deliberately did NOT touch `FRAMING_MANUAL`/conversation-view wrappers, which have the same no-auto-send behavior but weren't part of the request. Verified with `node --check config.js`. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-22 (per-message auto-inject mode split into independent GM/project settings) — Follow-up: the user asked for the same "when to auto-inject" option to apply separately to project instructions, with its own setting for when (and whether) it also injects on every request. Split the single `ccb_autoInjectMode` key into `ccb_autoInjectModeGm` + `ccb_autoInjectModeProject`, each independently `"start"`/`"every"`. `content.js#getAutoInjectMode(source)`/`setAutoInjectMode(source, mode)` now take `"gm"`/`"project"`; `loadAutoInjectMode()` loads both keys in one call and migrates from the old single key if the new ones are unset (seeds both, so an existing choice isn't silently lost). Advanced Options now has two selects (`#ccb-auto-inject-mode-gm`/`-project`); each card's live badge (`chat-features.js#renderGeneralMemory`, `history-view.js#renderProjectInstructionsCard`) reads/flips only its own source's mode. Core logic in [chat-features.js](chat-features.js): `_autoInjectPayload()` (conversation-start path) now excludes a source if its OWN mode is `"every"`; `buildPerMessagePrefix()` (per-message path) now includes a source only if its OWN mode is `"every"` — so GM and project instructions are handled by whichever of the two paths matches their individual mode, independently. `tryAutoInject()`'s old blanket `if (_isEveryMode()) return` was removed entirely (no longer correct once mixed states are possible) — it now relies solely on `_autoInjectPayload().text` being empty, which naturally happens once every remaining source is either absent or itself `"every"`. The send interceptor's gate became `_hasEveryModeSource()` (true if either source is `"every"`) instead of a single boolean. Verified with `node --check` on all touched files; browser pass pending (see Next Steps below, which folds into the existing per-message verification item).
- 2026-07-22 (per-message auto-inject mode) — User asked for GM + active-project instructions to load at the start of **every** user request, not just at conversation start. Added a global mode setting `chrome.storage.local["ccb_autoInjectMode"]` (`"start"` default / `"every"`), chosen via a new "מתי לטעון אוטומטית" select in Advanced Options (`#ccb-auto-inject-mode`) or by clicking the now-live auto-badge on the GM/instructions cards (`.auto-badge-live`, text "נטען בתחילת שיחה"/"נטען בכל הודעה"). In `"every"` mode, [chat-features.js](chat-features.js)'s new `installSendInterceptor`/`_interceptSend` catches the send action in the capture phase (send-button click or plain Enter in the chat input), prepends `buildPerMessagePrefix()` (GM in `<memory>`, instructions in `<project>`, wrapped in the new 6th framing pair `FRAMING_EVERY_PRE`/`POST` with `[[CCB:CTX]]`/`[[CCB:CTX-END]]` markers) **and lets the original event proceed** — the site's own handler sends the combined text. (First shipped as block-and-replay with a synthetic re-click; fixed same day — on Gemini the send listener is on the inner icon, not the wrapping `<button>`, so the synthetic click sent nothing: Enter needed a second press, mouse clicks did nothing. Second same-day fix: click detection was anchored on `document.querySelector`'s FIRST `SEND_BUTTON_SELECTOR` match, which on Gemini isn't necessarily the clicked icon (mic/send swaps, stale copies) — mouse sends went out unprefixed; now `_isSendClick()` resolves from the click target upward via `closest`. Third same-day fix: listeners moved to **`window` capture** (runs before document-capture). Fourth: detection was STILL missing — Gemini's send control isn't a `<button>` wrapping the selector's icon; `_isSendClick` gained a `[class*="send"]` ancestor heuristic, which finally fixed detection. Fifth: the hover/`pointerover` trigger added in fix #3 was **reverted** at the user's request — it filled the input on every stray mouse pass; with detection fixed, the ladder starts at `pointerdown` (mirrors Enter's keydown timing — press precedes the send-firing click). `console.log("[ccb] per-message context prepended via", e.type)` reports which event won; a temporary `[ccb-debug]` line logs undetected press/click targets.) `tryAutoInject()` early-returns in this mode (no start injection); `captureConversation()` strips the prefix up to `[[CCB:CTX-END]]` so saved conversations keep only the user's real text; `saveChat()`'s summary-prompt click also bypasses the interceptor. `prompts.js` gained the `everyIntro`/`everyOutro` editable pair (+ `framingEvery` reset key); `content.js` gained `loadAutoInjectMode`/`setAutoInjectMode` on the `docMaxChars` pattern, loaded in `init()` **before** the first `tryAutoInject()`. WHAT loads is unchanged — still the per-card `autoLoad` toggles; the mode only decides WHEN. Verified with `node --check` on all 8 touched files; browser pass pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-21 (removed the blocks-list empty state) — User asked that nothing at all be shown when there are no blocks. Deleted the `.empty` placeholder branch ("בנק ריק / לחץ + להוספת בלוק ראשון") from [content.js](content.js)#renderUnifiedBlocksList; the following `for` loop already renders nothing for an empty array, so `#list` just stays blank. The `.empty` CSS rule in [ui-styles.js](ui-styles.js) was **kept deliberately** — `history-view.js#renderHistoryList` still uses that class for "אין סיכומי שיחה שמורים" and "לא נמצא", which were out of scope. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md).
- 2026-07-21 (fix: one "new chat" click didn't auto-inject on the internal site) — User reported that on the internal AI, clicking "new chat" once didn't auto-inject the prompts; two clicks did. Root cause in [chat-features.js](chat-features.js): both state resets (`content.js#installNewChatBtnWatcher`'s delegated listener and the `ccb:urlchange` handler) are registered `capture: true`, so they run **before** the site tears down the current conversation — and `tryAutoInject` read `msgCount` **once**, right then, while the old chat's nodes were still in the DOM. It concluded "existing conversation", clicked new-chat itself, which re-entered the same capture listener → `tryAutoInject()` → same stale DOM → clicked again, recursing until the stack blew, injecting nothing. The second human click saw a settled DOM and worked. CLAUDE.md had **already documented** polling "until input is present and `msgCount === 0`" — it was never implemented; this change implements it. Replaced the one-shot read with `autoInjectTick()`, a 150ms poll: untimed wait for the input to mount (slow/login-gated sites), then `AUTO_INJECT_SETTLE_MS` (2.5s) of grace before a non-empty chat counts as genuinely existing, capped by `AUTO_INJECT_TIMEOUT_MS` (10s). Termination comes from clicking new-chat at most once per attempt (`_autoInjectClickedNewChat`) plus treating a re-entrant call as *resuming* the in-flight attempt rather than restarting its clock. Deleted the now-dead `_autoInjectObserver`. Verified with a 14-assertion Node `vm` suite on a virtual clock modelling the capture-phase ordering; the same suite against the pre-fix module raises `RangeError: Maximum call stack size exceeded`, confirming the diagnosis. Browser pass pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-21 (doc char cap → setting; conversation-meter perf) — Two user reports. **(a)** Asked whether 10,000 chars is enough and to make it configurable: added "מגבלת תווים למסמך" to Advanced Options (`#ccb-doc-max-chars`, thousands of chars, clamped 1–1000) stored at `chrome.storage.local["docMaxChars"]`, seeded from the new `config.js#DOC_MAX_CHARS_DEFAULT` — **default raised 10,000 → 50,000** (10K is only ~3–5K tokens). Wired on the `ctxWindow` pattern: `content.js#loadDocMaxChars`/`setDocMaxChars`, filled in `ui-modals.js#openSettings`, read via a `getDocMaxChars` dep in `history-view.js`. Code + structure docs stay fully exempt. **(b)** Reported that loading/UI slowness is "fixed" by toggling Chrome's hardware-acceleration setting but always returns — that toggle relaunches Chrome, so the *restart* was the fix, pointing at accumulation rather than the GPU. Root cause found in [ctx-meter.js](ctx-meter.js): `watchConversation()` used an **unthrottled** `subtree: true` MutationObserver, and `estimateTokens()` reads each message via the selector's `messageText()` → **`.innerText`**, which is layout-dependent and forces a synchronous reflow. Every mutation of a streaming response therefore re-read and re-scanned every message in the chat — cost proportional to conversation length, paid many times a second. Fixed with (1) a 400ms trailing throttle (`scheduleCtxMeterUpdate`, `setTimeout` not rAF; guard tests `!== null` since timer id `0` is falsy — the test suite caught that) and (2) a `WeakMap` per-message memo keyed on `textContent.length` (layout-independent) so only genuinely-changed messages pay for `innerText`. `cleanup()` clears the pending timer. Verified with a 37-assertion Node `vm` suite over the real modules. Browser pass pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-21 (fix: large code-project structure doc was truncated on injection) — User reported that for a very large file structure, the injected content "doesn't include all of it, cuts off in the middle." Traced to `history-view.js#runProjectDocumentsInjection`'s per-doc truncation: `const maxChars = doc.type === "code" ? Infinity : 10000` treated the auto-generated `PROJECT_STRUCTURE.md` (`type: "structure"`) as an ordinary text doc, so a large project's full file tree (easily >10,000 chars for a few hundred+ files) was silently cut mid-listing with a `"... [truncated]"` marker, even though `buildStructureMarkdown` itself always builds the complete tree — the loss was only at injection time. Fixed by exempting `type: "structure"` alongside `type: "code"` from the cap, same rationale already documented for code files: a partial tree looks complete but isn't, which is worse than the extra tokens. Verified with a small Node script simulating both doc types against 15,000-char content (structure/code pass through whole; ordinary text/word docs still cap at 10,000, unaffected). See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-21 (dep-graph accuracy + Hebrew-aware tokens) — User asked for a deep audit of the file-linking and token-estimation logic, then to fix all found bugs except unsupported-language gaps. Five dep-graph fixes in [dep-graph.js](dep-graph.js)/[history-view.js](history-view.js)/[code-tree.js](code-tree.js): (1) `scanRegions` no longer treats `//`//`/* */` as comments in Razor (`lang !== "razor"` gate) — markup URLs used to blank the rest of their line, silently dropping type references; (2) `CS_TYPE_RE` handles `record class`/`record struct` + `readonly`/`ref`/`new`/`unsafe`/`file` modifiers — `record struct Point` used to register the keyword "struct" as a symbol (false edges project-wide) and `readonly record struct` was missed; (3) the C# symbol table is built over strings-blanked text so verbatim strings can't register phantom types; (4) `joinRelative` returns null when `..` escapes the scanned root instead of remapping into the project; (5) `enableFilesForProject` returns the actually-matched count and `loadWithDependencies` reports it ("סומנו X מתוך Y" warning on a stale graph). Token estimation: new canonical `config.js#estimateTextTokens` — Hebrew chars ÷ 2, rest ÷ 3.5 (flat 3.5 undercounted Hebrew ~40%); [document-handler.js](document-handler.js) delegates to it and is now the single file-estimation implementation ([ctx-meter.js](ctx-meter.js)'s duplicated fork deleted — conversation meter sums the shared fn per message, per-file estimates call `docHandler.estimateFileTokens` at call time); `isLikelyTextFile` extension list aligned with `DEFAULT_CODE_EXTENSIONS` (uploaded `.cs`/`.cshtml`/etc. now extract text instead of blob+size/50); injection per-file token headers computed post-truncation; "טען קבצים" toast shows the total including the framing wrapper; code-tree counter excludes the structure doc from "נבחרים". Verified with a 35-assertion Node `vm` suite over the real files (Hebrew ratios, all five graph fixes, JS-resolution + comment-stripping regressions, code-behind pairing). Browser pass still pending. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-20 (timing logs for the render pipeline) — Follow-up to the entry below: the user tested it and reported the scan/sync steps all logged fast, but the panel still felt very slow afterward with **zero console output** during that slowness. Traced it to `_deps.render()` — every scan/inject flow ends by calling it, and neither `content.js#render()` nor `history-view.js#render()` had any timing, so a slow render looked identical to "stuck" in the console. Added `[ccb-timing] render` (`blocksListMs`/`historyViewMs`/`ctxMeterMs`/`totalMs`/`totalBlocks`) and `[ccb-timing] history-view.render` (`projectSelectMs`/`historyListMs`/`projectContextMs`/`totalMs`/`totalBlocks`/`conversations`). Leading suspect going in: `renderHistoryList()` rebuilds a DOM row for every stored conversation (and in content-search mode, re-scans every message of every conversation) on **every** render call — for an account with months of continuous auto-save (every message, 2.5s debounce), that cost is independent of the current project's file count, which would explain "slow even on a few dozen files." `totalBlocks`/`conversations` in the same log line makes that directly checkable without extra tooling. Same content-free rule as before — counts/ms only. Not yet confirmed against the user's actual numbers (waiting on their next test). See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-20 (timing-only logging for all file-loading paths) — User reported file loading is still slow and asked for detailed per-step timing logs, but with zero project content (no file/folder names, no file text) in the console — only which operations ran and how long each took. Replaced the leftover debug `[ccb-scan]`-prefixed logging (added 2026-07-19 to chase a scan-hang report, see below) with a consistent `[ccb-timing]` scheme across every file-loading path: `document-handler.js` (`scanCodeProject` — split into `discoverMs`/`readMs`, plus `filesFound`/`filesIncluded`/skip counts/`failedRead`; `syncCodeProjectDocuments` — `writeMs`/`saveMs`; `addDocument` — `readMs`/`blobMs`/`saveMs`; `injectFilesToChat` — `loadMs`), `history-view.js` (`createCodeProjectBookmark`, `rescanCodeProject` — `pickerMs`/`putMs`/`permMs`/`graphMs`/`saveMs`; `runProjectDocumentsInjection`, the "טען קבצים" flow — `codeContentsMs`/`extractMs`/`buildMs`/`injectMs`), `fs-handles.js` (`put`/`get`/`verifyPermission`), and `dep-graph.js` (`buildGraph`). Every payload is counts/ms/booleans/short enums only — the old logs' `dirHandle.name`, `handleName`, and per-file `name` were all removed, not just relabeled. Still one log call per phase/operation, never per-file, per the existing perf rule (see Key Patterns). See [ARCHITECTURE.md](ARCHITECTURE.md)'s new "Timing logs" subsection, [CHANGELOG.md](CHANGELOG.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (persistent progress indicator for file scanning/loading) — Follow-up to the perf work below: the user asked for a fixed indicator showing how many files have loaded so far and what's loading right now. Added `#scanProgress` below the global project bar (visible from either tab), showing phase label + `done / total` + a bar + the current file path. It exists because `setStatus` is a toast that auto-hides after 2.5s, so on a multi-minute scan it vanished while work was still running. Phases: "מאתר קבצים" / "קורא קבצים" / "בונה מפת תלויות" / "שומר קבצים" / "טוען קבצים לצ'אט". `content.js` owns `setProgress`/`clearProgress` (siblings of `setStatus`, passed into `history-view.js` deps); markup in `ui-template.js`, CSS in `ui-styles.js`. `scanCodeProject`'s `onProgress` changed from `(done, total)` fired every 25 files to an object `{ phase, done, total, current }` fired **per file, on both start and finish** — start-only reporting left the counter short by the number of in-flight reads (12), which froze the bar near the end of every scan. Four other non-obvious calls: throttle uses `setTimeout` not `requestAnimationFrame` (rAF is suspended in a backgrounded tab); the bar is indeterminate while discovery has no total; `clearProgress(holdMs)` has a sequence guard so a held hide can't blank a newer operation; `injectProjectDocuments` gained a try/catch wrapper since a throw would otherwise strand the indicator permanently. Verified in real Chrome via Playwright (28 assertions on the real markup/CSS/logic) + 16 assertions on the emitted event sequence over the `react-large` fixture; scan-output parity re-confirmed on all four fixtures. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (performance: large code-project scanning was taking many minutes) — A user reported that scanning a full-size code project took a very long time (it did complete, just extremely slowly) while small projects were fine. The cost was per-file overhead × thousands of files, in four independent places, all fixed with no behavior change. (1) **`scanCodeProject` now reads concurrently** — it was strictly sequential (`getFile()` → `text()`, one file at a time, sequential subdirectory descent), and since each of those is an IPC round-trip, almost all wall time was spent waiting. Split into a bounded BFS directory walk (`walkDirectories`, cap 8) + a bounded read pool (`runWithConcurrency`, cap 12). The traversal queue is **flat, not recursive** — recursing into a concurrency-limited helper per level would multiply the cap per level of depth (8^depth). `included` is now sorted by path since concurrent reads finish out of order. (2) **Removed the per-file/per-entry `[ccb-scan]` logging** added last session for the scan-hang investigation — 3–6 `console.log`s per tree entry is tens of thousands of calls on a large project, and each is expensive precisely when DevTools is open. One summary line remains. (3) **Batched storage IO** — `syncCodeProjectDocuments` fired one `set` per file, now batches 50 keys per call (`codeContentPutMany`); the new `docHandler.getCodeContents(docIds)` does one batched `get` for `injectProjectDocuments`, which previously paid a round-trip per selected file. (4) **`dep-graph.js#buildGraph` is now `async`** and yields every 25 files so the tab doesn't freeze; `scanRegions` is cached once per file (it ran up to 3× per C# file) and the combined type-name regex is compiled once per run instead of per file. Also added `onProgress` → status line ("סורק פרויקט... N קבצים" / "בונה מפת תלויות..." / "שומר קבצים..."). Verified with a Node harness on a synthetic 1,452-file tree: **47.6s → 4.1s**, 16 behavior assertions passing. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (search/filter box on the global scan-settings lists) — Each of the three chip lists in `#scanSettingsOverlay` (`denyDirs`/`denyFilenames`/`codeExtensions`) now has its own live filter input above it (`ui-modals.js#renderScanList`, case-insensitive substring, display-only — never touches `_scanDraft`). A chip's remove button still splices by the item's true index in the unfiltered array (captured in the same `forEach` that decides whether to render it), so filtering can't cause the wrong item to be deleted. Adding an item or clicking "אפס לברירת מחדל" clears that list's search box too, so a just-changed entry can't appear to vanish behind a stale filter. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md).
- 2026-07-19 (global code-project scan rules as editable settings) — The scan filters that were hardcoded in `document-handler.js` (`DENY_DIRS`, `DENY_FILENAMES_RAW`, `CODE_EXTENSIONS`, `MAX_CODE_FILE_SIZE`) are now user-editable global settings, pre-filled with their current values. New Advanced Options entry `#scanSettingsBtn` opens `#scanSettingsOverlay` (owned by `ui-modals.js#openScanSettings`/`closeScanSettings`/`saveScanSettings`/`resetScanSettingsToDefaults`) with three chip lists + a size input. Stored at `chrome.storage.local["ccb_scanSettings"]`, seeded on first load from the new `docHandler.getDefaultScanSettings()` and persisted immediately. `scanCodeProject()`'s second arg changed from `ignorePatterns[]` to a `{ ignorePatterns, denyDirs, denyFilenames, codeExtensions, maxFileSizeKb }` object; the old constants became `DEFAULT_*` seed values and `buildIgnoreMatchers`/`matchesIgnoreMatchers` became the general `buildPatternMatchers`/`matchesPatterns` (now also used for the deny lists). Edits are held in a module-scoped draft until Save, so Reset/close-without-save behave predictably. The per-project ignore list is untouched and still layers on top. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (fix: rescan reverted custom project name to folder name) — `history-view.js#rescanCodeProject` had `proj.title = dirHandle.name` after every rescan, so a code project the user had renamed (via `renameProject`) would silently revert to the folder's name the next time it got rescanned (manual refresh, or an ignore-pattern save, which reruns the scan). `createCodeProjectBookmark` already sets `title: dirHandle.name` correctly as a one-time default at creation — rescanning shouldn't touch the title at all, so the line was removed. Unrelated to `syncCodeProjectDocuments(proj, included, dirHandle.name)`'s `rootName` param, which still uses the folder name for the generated `PROJECT_STRUCTURE.md` header. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md).
- 2026-07-19 (manual ignore-list for code project scanning) — Added a way to manually exclude files/folders from code-project scanning. New `#codeProjectIgnoreBtn` (circle-slash icon, next to `#codeProjectRefreshBtn` in the documents header) opens `#ignorePatternsOverlay` (`history-view.js#openIgnorePatternsDialog`) where the user types file/folder names or `*`-globs (comma-separated for several at once); each is removable, and "שמור וסרוק מחדש" persists the list to `project.ignorePatterns` and immediately reruns `rescanCodeProject`. `document-handler.js#scanCodeProject(dirHandle, ignorePatterns = [])` gained the new param and a `matchesIgnoreMatchers()` helper (case-insensitive, matches bare name or full relative path, `*` wildcard) applied on top of the existing `DENY_DIRS`/`DENY_FILENAMES`. Both scan call sites (`createCodeProjectBookmark`, `rescanCodeProject`) now pass the project's `ignorePatterns`; new code projects default to `ignorePatterns: []`. No changes needed to `syncCodeProjectDocuments` — its existing "drop docs for files no longer in `included`" step already prunes newly-ignored files on rescan. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (fix: single-file toggle didn't sync inject button; project menu reverted to 3-dot at far left; "bookmark"→"project" wording; manual injection stops auto-sending) — Four items, all follow-up feedback on the previous session's changes: (1) **Fix** — a single file's checkbox in the code-project tree (`code-tree.js`) only called `updateTokenCount()`, never `_deps.render()`, so `content.js#syncInjectDocsBtn` (which now disables the footer button when nothing's selected) never re-ran for individual-file toggles — only folder-checkbox/select-all changes (which already go through `render()`) worked. Fixed by calling `_deps.render()`, matching the regular-project doc list's own per-file checkbox, which already did this; added `render` to `code-tree.js`'s deps. (2) **Reverted** — the two direct rename/delete buttons from last session are gone; back to a single `#projectEditBtn` 3-dot menu (`history-view.js#openProjectDropdown`, 2 items), now positioned as the LAST child of `#globalProjectBar` (far-left edge in RTL, past the "+" buttons — previously it sat between the selector and them). `renameProject`/`deleteProject` are unchanged as the underlying actions, just called from the menu instead of exposed as standalone buttons. (3) **Rename** — code-project delete now reads "מחיקת פרויקט" (delete project) instead of "הסרת סימניה" (remove bookmark); the internal `fs-handles.js`/`createCodeProjectBookmark()` naming is untouched, only user-facing text changed. (4) **Fix** — `injectSelected()` ("טען פרומפטים") no longer auto-sends after injecting, matching every other manual-load action in the panel. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (project tag beside title; 3-dot project menu → direct rename/delete buttons; file-inject button disables on empty selection) — Three items, all in response to user feedback on the previous session's changes: (1) **Fix** — `.ctx-proj-tag` now sits directly beside the block title (`.block-head` changed from `justify-content: space-between` to `flex-start`) instead of floating to the row's far edge, a leftover from when the row also held a since-removed tag-count badge. (2) **Redesign** — `#projectEditBtn` (the "3-dot" dropdown trigger) is gone; `#projectRenameBtn`/`#projectDeleteBtn` now act directly, no menu. The old dropdown's "load all"/"refresh" items (code projects only) were dropped as redundant with `#codeProjectRefreshBtn` and the new per-folder select-all checkbox — `loadCodeProjectAll()` was deleted outright since nothing calls it anymore. Rename now works for code projects too (previously regular-project-only). `history-view.js#deleteProject` branches by `project.isCodeProject` but both paths now share `unlinkProjectChildren()`, which also fixes a pre-existing bug: deleting a code project used to leave its conversations still tagged with the now-dead `projectId` (the regular-project path already unlinked them). (3) **Fix** — the footer "טען קבצים" button now *disables* (not just shows) when a project has documents but none actually enabled, since a code project's auto-generated `type:"structure"` doc is always `enabled:true` and invisible in the file tree, so clearing every real file selection previously still left the button clickable. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (fix: auto-inject skipped on refresh; folder select-all; documents→files rename; project-picker redesign) — Four items: (1) **Bug fix** — `content.js#initModules`'s `chat.init({ config: {...} })` never passed `NEW_CHAT_BTN_SELECTOR`, so `tryAutoInject()`'s "land on an existing non-empty chat → click new-chat myself, retry" fallback always saw `undefined` and silently skipped injection. This meant a page refresh that restored your last (non-empty) conversation would NOT auto-load GM/project instructions. Added the missing key. (2) **Feature** — `code-tree.js` folder rows now carry their own checkbox (checked/unchecked/indeterminate over descendant files, via new `collectDocs(node)`) to select/clear an entire folder in one bulk save; `history-view.js#enableFilesForProject` gained a third `enabled = true` param to support both directions. (3) **Rename** — every user-facing "מסמכים"/"מסמך" (documents/document) string became "קבצים"/"קובץ" (files/file) — footer button, section header, add-file dialog, status messages — internal field/function names (`documents`, `addDocument`, etc.) untouched. (4) **Redesign** — the "שייך לפרויקט" (assign conversation to project) picker (`ui-modals.js#showProjectPicker`) now reuses `.project-select-item` styling (same as the global project-selector dropdown, incl. folder icons for code projects) instead of its own bordered-button look, inside a bordered scrollable container (`max-height:280px`) so long project lists scroll properly. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (fix: instructions checkbox couldn't inject; code projects missing from assign-picker) — Two bugs found right after the checkbox change above shipped: (1) `historyView.init()` in `content.js#initModules` never received `updateInjectBtn` in its deps, so `renderProjectInstructionsCard`'s checkbox handler's `_deps.updateInjectBtn()` call threw silently — the selection updated internally but the footer button stayed visually disabled. Fixed by adding `updateInjectBtn` to that deps object. (2) The "שייך לפרויקט" picker (`ui-modals.js#showProjectPicker`) only ever listed regular projects because `content.js` wired its `getProjects` dep to `historyView.getProjects()` (regular-only) instead of `historyView.getAllProjects()` (regular + code) — switched the wiring. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-19 (instructions checkbox, not button; save-chat button removed; footer buttons matched; row layout fix) — Replaced `#injectInstructionsBtn` (previous session's standalone upload button) with a select-for-inject checkbox on the project-instructions card, matching the GM card's pattern exactly — manual load is now "tick the checkbox, click footer 'טען פרומפטים'" for both GM and instructions, one consistent flow. `injectSelected()` in `chat-features.js` simplified to drop its (previously unreachable) documents branch for `kind:"project"` blocks. Removed the "שמור שיחה" button (`#summarizeBtnHistory`) and its now-empty footer — `chat-features.js#saveChat()` logic is untouched, just unwired from any UI control (continuous auto-save is unaffected and remains the primary save path). Footer's two buttons (`#injectBtn`/`#injectDocsBtn`) are now equal width with clearer labels ("טען פרומפטים" / "טען מסמכים"). Fixed a layout bug where `.block` rows (plain context blocks) lacked `display:flex`, so the checkbox and title stacked instead of sharing a row — now `display:flex` like the GM/instructions cards' header already was. Dropped the "N תגים" count badge next to a block's title (tags themselves still render). See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (manual load-instructions button; "+" moved into header row) — New `#injectInstructionsBtn` (`history-view.js#injectProjectInstructions()`) manually loads the active project's instructions into the chat input (no auto-send), shown only when instructions are non-empty. `#addBtn` moved from its own toolbar row into `#blocksSection`'s header (`.section-head-right`), restyled to match `#addProjectBtn`/`#addCodeProjectBtn`/`#projectEditBtn` (small square, light bg). Removed the now-empty `.context-toolbar` wrapper and its dead CSS override. See [CHANGELOG.md](CHANGELOG.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 ("+" button restyled, moved left) — `#addBtn` shrunk 36→28px, rounded-square→circle, subtle shadow + hover scale. `#pane-context .context-toolbar` gained `justify-content: flex-end`, which is the visual left in this RTL layout — History's `#historyToolbar` untouched. See [CHANGELOG.md](CHANGELOG.md).
- 2026-07-19 (Context tab moved to the right) — Swapped the two `.tab` buttons' DOM order in `ui-template.js` so Context is right, History is left (RTL layout — first flex child sits at the right edge). No JS changes needed. See [CHANGELOG.md](CHANGELOG.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (removed continue + project-instructions checkbox; load-selected doesn't auto-send) — Deleted "המשך שיחה" entirely (not hidden): `#cvContinueBtn`, `pendingContinue`/`executeContinue`/`proceedWithContinue`/`waitForInput` in `content.js`, `state.continuationBase`/`state.suppressNextUrlReset`, the `is-active-label` CSS. Deleted the "כלול הנחיות פרויקט" checkbox: `#cvProjectBar`/`#cvIncludeProject`, `history-view.js#buildProjectSectionText`, `buildConversationInjectionText`'s `includeProjectInstructions` option (signature simplified to `(messages)`). `cvLoadBtn` ("טען נבחרים") no longer auto-sends — same pattern as the earlier `injectProjectDocuments()` fix. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (project selector rebuilt as custom dropdown) — Replaced the native `<select>` with `#projectSelectBtn`/`#projectSelectDropdown`, a fully custom dropdown matching the app's own styling. Code projects now show a real SVG folder icon (the `[קוד]` text-tag workaround is gone). New `history-view.js` functions: `renderProjectSelectItems`, `open/close/toggleProjectSelectDropdown`, `selectProjectFromDropdown`. `content.js` closes the dropdown on tab switch and panel close in addition to its own click-outside/Escape handling. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (project selector: text tag not emoji) — Replaced the `📁` prefix on code projects in `#projectSelect` with a plain `[קוד]` text tag. `U+FE0E` (text-presentation variation selector) doesn't work on this emoji — it only affects a fixed legacy character set, not modern pictographs like the folder emoji — so a genuinely flat/monochrome result requires plain text, not a "fixed" emoji. Native `<option>` also can't hold an SVG icon at all. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (file loading doesn't auto-send) — `injectProjectDocuments()` no longer clicks send after loading; the user reviews/edits/sends manually now. Removed the now-dead `sendButtonSel` dep from `content.js`→`history-view.js#init`. Also corrected a pre-existing doc inaccuracy noticed while touching this function: `injectFilesToChat` was documented as being called here, but it's never called anywhere — noted as an actual unfixed gap, not fixed in this pass. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (flat icon + file-injection framing) — Replaced `icon.png` (was a 601 KB non-icon raster) with a flat 128×128 icon matching the panel's palette; source is `icon-source.svg` (rendered via `sharp`, not added as a project dependency). Fixed the settings menu's "עריכת פרומפטים" item using a raw `✏️` emoji instead of a flat SVG like its siblings — now uses the already-defined `IC.pencil`. Added a 5th framing pair, `FRAMING_DOCS_PRE`/`POST` (`<documents>` tag), editable in the prompts editor — `history-view.js#injectProjectDocuments()` now uses it instead of reusing FRAMING_MANUAL with hardcoded tags. Deliberately did NOT touch the two other `<documents>`-building call sites (`chat-features.js#injectSelected`, `history-view.js#buildProjectSectionText`) since those are nested inside a different outer wrapper already. Added `"Files loaded."` to `INJECTION_AUTORESPONSES` in both files. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (documents header matches prompts header) — `#projectDocumentsCard`'s header now uses the same `.section-header` markup as `#blocksSection`'s ("פרומפטים להזרקה"), dropped its card background/border. Removed the standalone `#codeProjectInfoRow` info box; its refresh button moved into the header itself (`#codeProjectRefreshBtn`, code projects only, last-scanned age now a tooltip instead of visible text). `ui-styles.js` cleanup: removed `.code-project-info-*`/`.project-documents-card`/`-header`/`-head`, added `.section-head-right`, renamed the refresh-button style to `.doc-refresh-btn`. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (instructions editing = GM editing) — Replaced the project-instructions inline accordion with `history-view.js#renderProjectInstructionsCard()`, a near 1:1 port of GM's own render function — same `.gm-card` structure, same `autoLoad` toggle. Neither card has an edit button anymore; clicking the card opens the shared `openEdit()` form instead. Caught and fixed a would-be data-loss bug in the process: `saveEdit()` used to only preserve `kind`/`autoLoad`/`projectId` on save, which would have silently wiped a project's `documents`/`isCodeProject`/`dirHandleId`/`lastScanned`/`depGraph` the first time someone edited its instructions through the shared form. Rewrote it to spread the existing block first. Also hid `#deleteBtn` for `kind:"project"` blocks in `openEdit()` — project deletion must stay on `history-view.js#openProjectDropdown`'s dedicated (child-cleanup-aware) path. Removed `saveProjectView()`/`syncProjectInstructionsSection()`/`state.projectInstructionsOpen`. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (ASP.NET Razor support) — `.cshtml`/`.razor`/`.json` added to `CODE_EXTENSIONS` (`document-handler.js`), so ASP.NET view/component files and JSON config now get scanned in code projects (previously only `.cs`). `dep-graph.js` gained `analyzeRazorFile()`: Razor files reuse the C# symbol table (any known type name referenced in the markup, filtered by `@using`), a new `lang: "razor"` comment-stripping branch in `scanRegions()` for `@* *@`/`<!-- -->`, and guaranteed bidirectional code-behind pairing (`Foo.cshtml`↔`Foo.cshtml.cs`, `Foo.razor`↔`Foo.razor.cs`) in `buildGraph()`. Verified with a standalone Node smoke test (symbol resolution, `@using` filtering, comment stripping, code-behind pairing all correct) but not yet exercised through the actual extension UI on a real ASP.NET project. See [CHANGELOG.md](CHANGELOG.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (fold prompts into one collapsible) — `#blocksSection`'s collapse now hides the "+" toolbar, `#gmCard`, `#projectInstructionsCard`, and `#list` together (new `#blocksSectionBody` wrapper), not just `#list`. Pure template/CSS change (`ui-template.js`, `ui-styles.js`) — `content.js#syncBlocksSection` and its `#blocksSection`/`#blocksCollapseBtn` targets were unaffected since the collapse trigger element didn't move. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (reorder Context tab) — `#projectInstructionsCard` moved to directly below `#gmCard` (was above the "+ new block" toolbar). Pure template reorder, no id/wiring changes. See [CHANGELOG.md](CHANGELOG.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (Context tab polish) — Removed the project-instructions content preview (and dead `.gm-preview` CSS); moved `#projectInstructionsAutoBadge` below the header to match GM's layout; fixed `#projectDocumentsToggle` having no click handler (was un-collapsible — new `syncProjectDocumentsSection()` + `state.projectDocumentsCollapsed`); made the injectable-blocks list (`#list`) collapsible via new `#blocksSection`/`syncBlocksSection()`; fixed the code-project search input rendering with unstyled/LTR-biased browser chrome by giving it `.code-tree-search-input` folded into the shared search-input CSS. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (panel default tab → Context) — Swapped which tab/pane carries `.active` in `ui-template.js` so the sidebar opens to Context instead of History. Fixed a latent bug this exposed: `content.js#setPanelOpen` unconditionally focused `#searchHistory` (History-only field) on every open; now guarded to History-tab-active only. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (project-instructions auto-load toggle) — Project auto-load is now an explicit per-project toggle (`project.autoLoad`, new `#projectInstructionsAutoToggle`) instead of being implied by having an active project. Missing `autoLoad` defaults to ON (no migration needed). The "נטען אוטומטית" badge now reflects the toggle, not content presence — shows even when instructions are empty. `chat-features.js#_getActiveProjectInstructions` gates on the toggle before returning text. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-19 (debug logging for code-project scan hang) — Added temporary `[ccb-scan]`-prefixed console logging across the whole scan pipeline to diagnose a bug report: on a second machine, code-project scanning gets stuck on "סורק פרויקט..." forever with no console error, even for a tiny folder (rules out folder size/OneDrive-cloud-placeholder theories). Logged: `history-view.js` picker/handle-store/scan/sync steps, `document-handler.js#scanCodeProject`'s `walk()` per-directory/per-entry/per-file, `#syncCodeProjectDocuments`'s content-write batch, and `fs-handles.js` IndexedDB + permission calls. Next step is to reproduce on the affected machine and read where the log stream stops. Not yet committed — see [CHANGELOG.md](CHANGELOG.md).
- 2026-07-19 (project instructions auto-load) — The active project's instructions now auto-inject at conversation start alongside General Memory; either source alone triggers it. `chat-features.js` gained `_getActiveProjectInstructions()` + `_autoInjectPayload()` (payload computed at inject time, not call time, since the fallback observer has no timeout). New `#projectInstructionsAutoBadge` in `ui-template.js`, toggled by `history-view.js#renderProjectContext`. Deliberately instructions-only — documents remain behind the explicit "מסמכים" footer button. See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-16 (Context-tab UX redesign) — Unified the two project concepts: retired `kind:"ctx-project"` and folded it into `kind:"project"` via an idempotent `content.js#migrateCtxProjects()` in the load path. Restructured `#pane-context` (`ui-template.js`): the context-window meter, then a segmented sub-view control (`.ctx-subview-toggle`) with "טקסטים כלליים" (general blocks + GM) and "פרויקטים" (the unified project list + inline project detail — instructions, own text blocks, documents, code-project entry points). Moved all project UI out of the History tab (now conversations-only). In `content.js`: added `state.ctxSubview` + `syncCtxSubview()`, split `renderContextList()` into general-only + `renderProjectBlocksList()`, removed `createCtxProject`/`renameCtxProject`/`deleteCtxProject`. In `history-view.js`: merged `renderProjectList`/`renderCodeProjectList` into one, added `getAllProjects()` + `syncCtxProjectsLayout()`, dropped the History project section. Restyled the `#addDocumentOverlay` dialog (design-system CSS, no inline styles). Bumped `SIDEBAR_WIDTH` 300→340 (`config.js`). See [CHANGELOG.md](CHANGELOG.md), [ARCHITECTURE.md](ARCHITECTURE.md), [CLAUDE.md](CLAUDE.md).
- 2026-07-16 (earlier) — Added project documents (`document-handler.js`, new) and code-project scanning + static dependency graph (`dep-graph.js`, `code-tree.js`, `fs-handles.js`, all new). `manifest.json` gained `unlimitedStorage` + the four new scripts. `ctx-meter.js` binary-token estimation extended (more file types, async) and reused by `document-handler.js`.

## Entry Points
- `manifest.json` — content-script load order (source of truth for module boot sequence)
- `content.js#init` — extension entry point; mounts the Shadow DOM UI and wires every module
- `content.js#initModules` — builds the shared `deps` object and calls each module's `init(deps)`

## Key Patterns Used
- Every UI module exposes `init(deps)` and stashes deps privately; state mutations go through `_deps.state` / `_deps` helpers, never directly on `window.__ccb*` (see [CLAUDE.md](CLAUDE.md) Rules section).
- Bulk mutations (e.g. `enableFilesForProject`, `setAllCodeDocsEnabled`) call `saveBlocks()` once, not once per item — looping per-item saves was a real perf bug (each save re-serializes the whole `blocks` object).
- Large or numerous text payloads (file blobs, per-file code content) are kept out of the `blocks` object entirely, in their own `chrome.storage.local` keys, so routine `saveBlocks()` calls (toggling a checkbox, renaming something) stay cheap regardless of project size.
- Anything that touches N files does bounded-concurrency or batched IO, never one round-trip per file: file reads go through a concurrency pool, storage writes are batched per 50 keys, storage reads are one `get` for the whole set. Every one of these is an IPC hop to the browser process, so a per-file loop is latency-bound and scales terribly (this was a real bug — see the 2026-07-19 perf entry).
- Per-file `console.log` in hot paths is treated as a performance bug, not just noise: it's most expensive exactly when DevTools is open, which is when someone is diagnosing slowness. All timing logs use a fixed `[ccb-timing]` prefix, one call per phase/operation (never per-file), and carry ONLY counts/ms/booleans/short enums — never a file/folder name or file content (2026-07-20).
- Static analysis over AI: the dependency graph is a deterministic, no-API-call heuristic — intentionally conservative (favors including too much over silently missing a file).

## Next Steps
- [ ] Manually verify the per-file dependency manager: open a scanned code project's file tree, click the "אפשרויות תלויות" (link icon) button on a file that has known imports, confirm a new "ניהול תלויות" item appears below a separator under the existing "תלויות"/"תלויים"/"הקשר מלא" options, and click it. Confirm a full-pane view opens showing the file's outgoing dependencies as rows each with a remove (X) button, an "הוסף תלות" search input below them, and a read-only "תלויים נכנסים" list at the bottom. Remove one dependency, confirm it disappears immediately; type into the add box and confirm filtered file suggestions appear (excluding the file itself and files already listed), click one and confirm it's added. Close the view (back button or Escape) and reopen the SAME file's manager — confirm your edits persisted. Rescan the whole project (refresh button) and reopen this file's manager again — confirm your manual edit is STILL there and still overrides whatever the fresh scan would have said (the core requirement: manual choices survive and win over the rescanned default). Open the file's "load with dependencies" menu again and confirm the "(N)" counts reflect your edit too, not just the manager screen. Open the conversation-preview or another file's preview while the manager is open and confirm only one full-pane view is ever visible; close the whole panel and reopen it, confirm the manager isn't stuck open underneath.
- [ ] Manually verify the file preview header fixes: open a file preview and confirm the top-right button is now an X (not a chevron), clicking it closes the view same as before. Confirm the header shows the bold file name on top and the FULL path (including the file name) as a small dim line below it. Confirm the token/char line reads correctly left-to-right ("180 tokens · 683 תווים", not "תווים 683") — this is the one part of this change that couldn't be confirmed outside a real browser (Unicode bidi rendering).
- [ ] Manually verify the full-pane file preview: open a scanned code project's file tree, click the eye-icon button on a small file and confirm it opens a full-pane view (same takeover style as the History tab's conversation preview) showing the file's content in monospace, with a meta line reporting a token count and character count. Click the back button (or press Escape) and confirm it closes. Open a large file (>20,000 chars) and confirm the body is truncated but the meta line still reports the file's TRUE full length plus a "מוצגים ... תווים ראשונים" note. Open the file preview, then switch to the History tab and open a saved conversation's preview — confirm only one full-pane view is ever visible at a time (opening one closes the other). With the file preview open, switch Context/History tabs and confirm it closes; close the whole panel and reopen it, confirm the preview isn't stuck open underneath. Try previewing a file that no longer exists on disk (renamed/deleted since the last scan) and confirm a "לא נמצא תוכן לקובץ" error toast appears without a blank/broken view opening.
- [ ] Manually verify the execCommand→Range.insertNode fix on the affected machine (supersedes the "load-files-then-load-prompts hang fix" item below, which verified the now-replaced execCommand-based point-insertion): scan/select a code project with hundreds of enabled files (enough that "טען קבצים" previously took tens of seconds or longer), click "טען קבצים", and confirm it completes in low single-digit seconds rather than freezing the tab. Confirm the injected text's line breaks render correctly (not collapsed onto one line) and that clicking "טען פרומפטים"/"טען נבחרים" right after still works instantly with correct final content and cursor position.
- [ ] Manually verify the deps-menu counts + folder search: open a scanned code project, click the deps ("link") button on a file that has known imports and confirm "תלויות (N)"/"תלויים (M)"/"הקשר מלא (K)" show plausible numbers matching what actually gets marked when you click each option. Confirm a leaf file (no imports, nothing imports it) shows "(0)" for all three. Confirm a project that hasn't been scanned yet shows the menu labels with no parenthesized count at all (not "(0)"). Then type a folder name into the tree's search box and confirm every file under it appears, and that a nested path query (e.g. `src/utils`) narrows correctly without matching unrelated similarly-named files.
- [ ] Manually verify the "טען קבצים" exact-BPE fix on the affected machine: scan/select a code project with hundreds of enabled files, click "טען קבצים", and confirm the panel responds without a multi-second-to-minutes stall. Confirm the per-file token labels in the injected text still look reasonable (heuristic-based, ~1.8% aggregate error) and that the final "קבצים נטענו (~N tokens)" success toast is still the exact count.
- [ ] Manually verify the load-files-then-load-prompts hang fix: on the active site, scan/select a code project with a large enough set of files that the injected content is at least a few hundred KB, click "טען קבצים", then immediately click "טען פרומפטים" (with at least one block selected) and confirm the panel responds instantly with no freeze. Repeat with "טען נבחרים" (load selected conversation messages) as the second action. Confirm the final text in the chat box is correct in both content and order (new content where expected, old content preserved) — not just fast.
- [ ] Manually verify the scan-speed fix: scan/rescan a real code project and confirm it feels roughly as fast as before the tokenizer landed (no multi-second stall added per hundred files). Confirm the file tree's per-file token numbers still look reasonable (heuristic-based, ~1.8% aggregate error) while a single file's live count (e.g. via the conversation meter on pasted content) is exact.
- [ ] Manually verify the token-budget bar: open a code project, tick files one at a time and confirm the bar under the file tree grows smoothly and its percentage matches `tokens / context window`. Confirm it turns amber past ~50%, orange past ~75%, and red (pulsing) past ~90% — the same colors the top meter shows at equivalent fill. Tick enough to exceed the window and confirm it clamps at 100% rather than overflowing. Open a *regular* (non-code) project with documents and confirm the same bar appears under that flat list. Set the context window to a very small number in Advanced Options and confirm both bars react.
- [ ] Manually verify the real tokenizer: open DevTools on a chat-site tab and confirm `[ccb-timing] tokenizer.load { ranks: 199998, totalMs: ~200-400 }` appears shortly after load, with **no** CSP or `web_accessible_resources` error. Confirm the context meter's number on a Hebrew conversation *drops* relative to before (the heuristic over-counted Hebrew: measured 691 real vs 853 heuristic on a sample) and that an English/code conversation drops substantially more (391 vs 549). Open a code project's file tree and confirm the token sum is plausible and updates as files are ticked. Confirm a non-chat tab (any random site) does **not** fetch the 3.4 MB file (Network tab, filter `tiktoken`). Then confirm graceful degradation: temporarily rename `tokenizer/o200k_base.tiktoken`, reload the extension, and verify the panel still works with heuristic numbers and logs `[ccb] tokenizer load failed, using heuristic:` rather than breaking.
- [ ] Manually verify CSS `@import` support: scan a code project containing `.css` files with real `@import`s (plain, `url(...)`, quoted and unquoted, one with a trailing media query, one pointing at a CDN URL), confirm `.css` files show up in the file tree same as before, and that "load with dependencies" ("תלויות"/"תלויים"/"הקשר מלא") on one of them correctly pulls in the imported CSS files (not the CDN URL, not anything commented out). Also confirm a `.js`/`.cs` file elsewhere in the same project still resolves its own imports correctly (no regression from the `scanRegions` change).
- [ ] Manually verify the reworded file-loading ending: open a project with enabled documents/files, click the footer "טען קבצים" button, confirm the injected text still ends with the new "End of the attached files. The user's actual request follows — respond to it only." line (not the old "Reply only with 'Files loaded.'"); type a real question after the injected block and send — confirm the AI answers the actual question directly in one turn (no separate "Files loaded." acknowledgment first). Open the prompts editor and confirm the "5) מעטפת הזרקת קבצים" outro field shows the new default wording.
- [ ] Manually verify the per-message auto-inject mode, now split per source: (a) Advanced Options shows TWO selects — "מתי לטעון זיכרון כללי" and "מתי לטעון הנחיות פרויקט" — each independently defaulting to "בתחילת שיחה"; (b) switch ONLY the GM select to "בכל הודעה" (via the select AND via clicking the GM card's badge — both flip it, GM badge text updates, the PROJECT card's badge stays unchanged); type a message with a project active, send with the send button — the sent message should carry ONLY the GM `<memory>` block (not `<project>`), and the AI should answer the actual question; (c) same for Enter; Shift+Enter still makes a newline; (d) with GM in "every" mode and project instructions in "start" mode, a fresh conversation should auto-inject the project's instructions ONCE at start (GM should NOT also appear there — it rides per-message only); (e) now flip the PROJECT select to "every" too (GM still "every") — a fresh conversation gets NO start-injection at all, and every sent message carries BOTH `<memory>` and `<project>`; (f) flip project back to "start" alone — confirm mixed mode still works correctly (GM per-message, project at start); (g) the saved conversation in History shows only your real text (no `[[CCB:CTX]]` block) regardless of which combination was active; (h) "טען פרומפטים"/"טען קבצים" manual injections are not double-wrapped when sent; (i) flip both back to "בתחילת שיחה" and confirm the original combined-start-injection behavior resumes exactly as before the split.
- [ ] Manually verify the single-click auto-inject fix **on the internal site** (`ACTIVE_SITE = "internal"` in `config.js`): with GM auto-load on, sit in a conversation that has messages, click "שיחה חדשה" **once**, and confirm the prompts are injected and sent without a second click. Repeat from a page refresh that restored an old conversation (the extension should click "new chat" itself after ~2.5s, then inject). Confirm the console shows no `RangeError`/stack-overflow. Then re-check Gemini for no regression — both the plain "open gemini.google.com" path and clicking Gemini's own new-chat link.
- [ ] Manually verify the conversation-meter perf fix (the important one): open a long Gemini chat, let a long response stream in, and confirm the tab stays responsive throughout and the meter still updates (about twice a second, not per keystroke of the stream). Then keep the same conversation going for a while and confirm it does **not** progressively degrade — that was the symptom that a browser restart used to mask. Optional sanity check in DevTools → Performance: recording during a stream should no longer show a dense wall of forced-reflow ("Recalculate Style"/"Layout") entries attributable to the extension.
- [ ] Manually verify the new "מגבלת תווים למסמך" setting: open Advanced Options, confirm it shows 50 (thousand) by default, change it to e.g. 5, save, then inject a document longer than 5,000 chars and confirm it truncates with `... [truncated]`; raise it back and confirm the full text is injected. Confirm a code project's files and `PROJECT_STRUCTURE.md` are never truncated regardless of the value.
- [ ] Manually verify the structure-doc truncation fix: scan a code project large enough that `PROJECT_STRUCTURE.md` exceeds 10,000 characters (several hundred files), click "טען קבצים", and confirm the injected structure section shows the complete tree (down to the last file, no `"... [truncated]"` marker) instead of stopping partway through. Confirm an actually-huge non-code text document (a pasted/uploaded doc, not structure) still truncates at 10,000 chars as before.
- [ ] Manually verify the 2026-07-21 dep-graph/token fixes in the browser: (a) context meter on a Hebrew Gemini chat should read noticeably higher than before (~1.5-2×) and closer to reality; (b) upload a `.cs` file to a regular project — it should show a real token estimate (not `size/50`) and inject as text, not as an attachment; (c) scan the ASP.NET project, pick a `.cshtml` with a type reference on the same line as an `https://` URL and confirm "תלויות" pulls that type's file in; (d) a project using `record struct` should no longer pull unrelated files into every closure; (e) clear all selections in a code project — counter should read "0 קבצים נבחרים" (not 1); (f) "טען קבצים" toast should show a total token estimate; (g) with a deliberately stale graph (rename a file on disk, don't rescan), "תלויות" should report "סומנו X מתוך Y".
- [ ] Get the user's actual `[ccb-timing] render` / `[ccb-timing] history-view.render` numbers from their next test — specifically `historyListMs` vs `totalBlocks`/`conversations`. If `historyListMs` scales with `conversations` and is the dominant cost, the fix is likely capping/paginating `renderHistoryList()` or lazily rendering rows instead of rebuilding the full list on every `render()` call. If it's small even with many conversations, look at `projectContextMs`/`code-tree.js#renderInline` next (proportional to the scanned project's file count instead).
- [ ] Manually verify the new `[ccb-timing]` logs: open DevTools console, create/rescan a code project and confirm `[ccb-timing]` lines appear for `scanCodeProject`, `dep-graph.buildGraph`, `syncCodeProjectDocuments`, `fs-handles.put/get/verifyPermission`, and the wrapping `createCodeProjectBookmark`/`rescanCodeProject` line — each with plausible `ms` values and no file/folder name anywhere in the payload. Click "טען קבצים" on a project with files enabled and confirm `runProjectDocumentsInjection`'s line appears with `codeContentsMs`/`extractMs`/`buildMs`/`injectMs`. Add a regular document via "+ הוסף" and confirm `addDocument`'s line appears. This replaces the earlier (now removed) `[ccb-scan]` debug-logging investigation below — if the original scan-hang report resurfaces, the `[ccb-timing]` line that never appears (or never completes) now pinpoints the stuck phase instead of a raw log stream.
- [ ] Manually verify the progress indicator in the browser: start a scan on a large code project and confirm the strip appears below the project bar, sweeps during "מאתר קבצים", then counts up through "קורא קבצים" with the file path changing, reaches the total (does not stall short), passes through "בונה מפת תלויות"/"שומר קבצים", and finally holds a green "נסרקו N קבצים" for ~2.5s before disappearing. Confirm it is visible from the History tab too. Then trigger "טען קבצים" and confirm the "טוען קבצים לצ'אט" phase behaves the same. Cancel/fail a scan (e.g. revoke folder permission) and confirm it turns red and clears itself rather than sticking.
- [ ] Manually verify the scan performance work on the affected machine: scan a full-size code project and confirm it completes in a fraction of the previous time, that the status line visibly counts up ("סורק פרויקט... N קבצים") rather than sitting static, and that the tab stays responsive during "בונה מפת תלויות...". Then confirm output parity against the previous behavior: the same files appear in the tree, `PROJECT_STRUCTURE.md` lists the same paths, "load with dependencies" still resolves correctly, and "טען קבצים" loads the same content. Rescan an already-scanned project and confirm `enabled` states are preserved and removed files are pruned.
- [ ] Manually verify the scan-settings search boxes: open a list with many entries (e.g. denyDirs) and type a partial match, confirm only matching chips remain visible; clear the search and confirm everything reappears. Remove an item while a filter is active and confirm the correct chip disappears (not an unrelated one). Add a new item while a filter is active and confirm the search box clears so the new item is visible. Click "אפס לברירת מחדל" with an active filter and confirm the filter clears too.
- [ ] Manually verify the global scan settings: open Advanced Options → "קבצים לסריקת פרויקטי קוד" and confirm all four sections are **pre-filled with the current defaults** (not empty). Remove `package.json` from the files-to-exclude list, Save, then rescan a code project and confirm `package.json` now appears in the file tree. Add `txt` to the extensions list, Save, rescan, confirm `.txt` files get scanned. Click "אפס לברירת מחדל" and confirm the fields revert **without** persisting until Save is pressed (close without saving, reopen, confirm the edits are gone). Confirm a *second* code project also picks up the changes on its own next rescan (proving it's global). Confirm the per-project ignore dialog still works independently on top of these.
- [ ] Manually verify the rescan title fix: rename a code project via the 3-dot menu, then trigger a rescan (refresh button, or save an ignore-pattern change) and confirm the title stays as the renamed value instead of reverting to the folder name.
- [ ] Manually verify the ignore-list feature: open a code project, click the new circle-slash button next to the refresh button, add a folder name (e.g. an existing subfolder) and a glob (e.g. `*.test.js`), click "שמור וסרוק מחדש", and confirm the matching files/folders disappear from the file tree; remove an entry and rescan again to confirm they reappear. Also confirm the button's tooltip shows the current ignore count.
- [ ] Manually verify the project tag position: select a project, add a text block owned by it, and confirm the `.ctx-proj-tag` pill sits right next to the block's title (not floated to the far left edge of the row).
- [ ] Manually verify the 3-dot project menu: confirm `#projectEditBtn` sits at the far-left edge of the project bar (past the "+" buttons), click it and confirm a 2-item menu opens ("שנה שם" / "מחק"), not a lone action. Rename a regular project and confirm it applies; delete it and confirm the dialog reads "מחיקת פרויקט" and clears its own text blocks while unlinking (not deleting) its conversations. Repeat for a code project — confirm rename now works there too, delete also reads "מחיקת פרויקט" (not "הסרת סימניה"), releases the folder bookmark, and correctly unlinks its conversations.
- [ ] Manually verify the file-inject button disabling works for BOTH bulk and single-file selection: open a code project, clear every selection, confirm the footer "טען קבצים" button is disabled. Tick ONE individual file (not a whole folder) and confirm the button re-enables immediately (this was broken — only folder/select-all toggles used to refresh it). Untick it again and confirm it disables again.
- [ ] Manually verify manual "טען פרומפטים" no longer auto-sends: select a block (or GM/instructions), click the footer "טען פרומפטים" button, and confirm the text lands in the chat input WITHOUT being sent — you should be able to edit or add to it before sending yourself.
- [ ] Manually verify the auto-inject-on-refresh fix: open an existing (non-empty) Gemini conversation with GM auto-load and/or a project with auto-load instructions active, refresh the page, and confirm the extension clicks "new chat" itself and then auto-injects into the resulting blank chat (previously this silently did nothing). Also verify plain site entry (first navigation to gemini.google.com) and manually clicking Gemini's own "new chat" link both still auto-inject as before (these two paths were unaffected by the bug, should show no regression).
- [ ] Manually verify the per-folder checkbox in the code-project file tree: open a code project, tick a folder's checkbox and confirm every file under it (including nested subfolders) gets enabled in one action; toggle a couple of individual files inside it and confirm the folder checkbox becomes indeterminate (dash), then untick it and confirm all descendants clear. Try this again while a path search is active and confirm it only affects the currently visible/filtered files.
- [ ] Manually verify the "documents"→"files" rename: open a project's documents section and confirm the header says "קבצים" (not "מסמכים"), the footer button says "טען קבצים", the add dialog says "הוסף קובץ", and status toasts (loading/deleting/adding a file) all say "קובץ"/"קבצים".
- [ ] Manually verify the redesigned project picker: open a conversation row's dropdown → "שייך לפרויקט" and confirm the list now looks like the global project-selector dropdown (flat rows, hover highlight, folder icons on code projects) instead of separate bordered buttons; with many projects, confirm the list scrolls inside its own bordered box instead of growing the whole dialog off-screen.
- [ ] Manually verify the instructions checkbox (incl. the just-fixed bug): select a project with instructions, tick the checkbox on its instructions card, confirm the footer "טען פרומפטים" button un-disables and its count includes it, click it, and confirm the instructions text (not documents) lands in the chat input WITHOUT sending. Switch to a different project and confirm the previous project's checkbox state doesn't silently carry over (it should be pruned from the selection). Confirm GM + instructions can be selected together and inject as one combined block.
- [ ] Manually verify the "assign to project" picker (just-fixed bug): open a conversation row's dropdown → "שייך לפרויקט" and confirm BOTH regular and code projects appear in the list, not just regular ones; assign a conversation to a code project and confirm it filters correctly under that project in the History list afterward.
- [ ] Manually verify the save-chat button removal: confirm the History tab's footer is gone entirely (no button, no dead space) and that ongoing auto-save (`scheduleAutoSave`) still saves/updates the current conversation as messages arrive.
- [ ] Manually verify the block-row layout fix: open the Context tab and confirm every plain block row (not GM/instructions) shows its checkbox and title on the same line, not stacked.
- [ ] Manually verify the repositioned "+": confirm "+" still opens the new-block form.
- [ ] Manually verify the "+" button: confirm it's smaller/circular and sits on the left side of its row in the Context tab, still opens the new-block form on click, and that the History tab's search toolbar layout is unaffected.
- [ ] Manually verify the tab order: open the panel and confirm "Context" sits on the right and "שיחות אחרונות" on the left, the sliding tab indicator lines up correctly under whichever is active, and clicking each still switches panes correctly.
- [ ] Manually verify the conversation-view changes: open a saved conversation's preview and confirm there's no "המשך שיחה" button and no "כלול הנחיות פרויקט" checkbox anymore (only "טען נבחרים" in the footer). Select some messages, click "טען נבחרים", confirm the text lands in the chat input WITHOUT sending. Confirm auto-save of the CURRENT chat still works normally afterward (unaffected — this only touched the continue/checkbox features, not ongoing auto-save).
- [ ] Manually verify the custom project dropdown: click the project trigger button and confirm it opens below it with all projects listed (code projects showing a folder icon), the active one highlighted; click a row and confirm it selects + closes; click outside and confirm it closes without selecting; open it, press Escape, confirm it closes; open it, switch tabs (Context↔History), confirm it closes; open it, close the panel, reopen, confirm it's not stuck open. Also confirm keyboard Tab-navigation reaches the trigger button reasonably (no full arrow-key roving focus was implemented — click/Escape only).
- [ ] Manually verify file loading doesn't auto-send: click "מסמכים" on a project with enabled documents and confirm the text lands in the chat input WITHOUT sending — you should be able to type before/after it and press send yourself.
- [ ] Manually verify the new icon: reload the unpacked extension in `chrome://extensions` and confirm the toolbar/extensions-page icon renders correctly (crisp at 16px, no artifacts) — only ever previewed via a standalone PNG render, never through an actual loaded extension.
- [ ] Manually verify the file-injection framing: open the prompts editor (Settings → עריכת פרומפטים, now with the flat pencil icon) and confirm a 5th "מעטפת הזרקת קבצים" section appears with its own intro/outro + reset button; edit it, save, then use the footer "מסמכים" button on a project with enabled documents and confirm the injected text uses the new wording; confirm the AI's canned "Files loaded." reply does NOT show up in the saved conversation afterward.
- [ ] Manually verify the documents header: open a regular project and confirm the "מסמכים" header looks like "פרומפטים להזרקה" (no card border/background) with "+ הוסף" on the right; open a code project and confirm "+ הוסף" is replaced by a refresh icon that rescans on click and shows the last-scan age as a tooltip on hover; confirm the collapse chevron still works for both.
- [ ] Manually verify Razor support: scan a real ASP.NET project through the extension UI, confirm `.cshtml`/`.razor`/`.json` files show up in the code-tree, and that "load with dependencies" on a Razor view pulls in the C# types it references plus its code-behind file (only verified so far via a standalone Node smoke test, not through the actual UI).
- [ ] On the affected second machine (if the scan-hang report resurfaces): reload the unpacked extension, open DevTools console on the chat page, trigger a code-project scan, and find the last `[ccb-timing]` line before it stops — that pinpoints the exact stalled call (picker / IndexedDB / `handle.entries()` / `getFile()` / `file.text()` / storage write). The `[ccb-scan]` debug logging this step originally referred to was removed 2026-07-20 in favor of the permanent `[ccb-timing]` instrumentation above.
- [ ] Manually verify project-instructions editing = GM editing: click the GM card and confirm it opens the shared edit form (not a separate accordion); click its checkbox/toggle and confirm that does NOT also open the form. Repeat for the project-instructions card, and confirm saving through the form doesn't wipe a code project's scanned files/documents (check `#projectDocumentsList` still has content after saving instructions). Confirm the edit form's delete button is hidden while editing a project (no way to delete a project from this form) but still shown while editing GM or a regular block.
- [ ] Manually verify Context-tab polish: open a project, confirm the "נטען אוטומטית" badge sits on its own line below the header (like GM's). Click the documents-section chevron and confirm it actually collapses/expands. For a code project, check the file-search box (`חיפוש לפי נתיב...`) now looks like the other search boxes and reads/types correctly in RTL.
- [ ] Manually verify `#blocksCollapseBtn`: clicking it should hide the "+" button, the GM card, the project instructions card, AND the blocks list all together; clicking again should restore all of them exactly as they were (GM toggle state, instructions accordion open/closed state, selected checkboxes).
- [ ] Manually verify default tab: open the panel fresh (click FAB / Ctrl+Shift+L), confirm it lands on Context (not History), the tab indicator sits under Context, and History still opens/works normally (incl. `#searchHistory` focus) when clicked.
- [ ] Manually verify project-instructions auto-load + toggle: select a project with instructions, open a fresh chat, confirm GM + instructions arrive in ONE message and the "נטען אוטומטית" badge/toggle show ON on the instructions card. Flip the toggle off, open another fresh chat, confirm instructions are NOT injected (GM still is, if applicable) and the badge disappears. Confirm an older project (no `autoLoad` field ever saved) still shows the toggle ON by default. Confirm documents are NOT included either way.
- [ ] Manually verify the Context-tab redesign in the browser: general/projects sub-view toggle, opening a project, adding a text block + a document, code-project scan/picker, and that a pre-existing `ctx-project` correctly migrates to a `project` (open the console — `migrateCtxProjects` runs silently in `loadBlocks`).
- [ ] Verify the History tab is conversations-only and the "assign to project" picker still lists the unified projects.
- [ ] Manually verify the code-project scan → dependency graph → file-tree inject flow (JS + C# — `mishley-test-projects/` has fixtures)
- [ ] Verify DOCX/ODT/RTF text extraction against real files
- [ ] Decide whether `mishley-test-projects/` (test fixtures) should be committed or added to a `.gitignore`
- [ ] Stage and commit the pending changes (see [CHANGELOG.md](CHANGELOG.md))

## Gotchas / Watch Out
- Stored `estimatedTokens` values written before 2026-07-21 used the flat 3.5 divisor — they refresh on the next scan (code files) or when a doc is re-added/re-extracted, NOT retroactively. A mixed panel (old + new estimates) is expected until the user rescans.
- There is exactly ONE chars→tokens policy: `config.js#estimateTextTokens`. Do not add a local `chars / 3.5` (or any ratio) anywhere — that duplication is the bug that was just removed. Same for file heuristics: `document-handler.js` is canonical, `ctx-meter.js` only wraps it.
- **Never read `.innerText` (or `offsetWidth`/`getBoundingClientRect`/`scrollHeight`) inside an unthrottled MutationObserver on the chat DOM.** Those properties are layout-dependent, so each read forces a synchronous reflow; a streaming AI response fires mutations continuously, so the cost multiplies by the whole conversation and shows up as the tab degrading over a session (2026-07-21). Use `textContent` where possible, memoize per element, and throttle the observer.
- Any throttle/debounce guard on a timer handle must test `!== null`, not truthiness — a `setTimeout` id of `0` is falsy and silently defeats the guard.
- `content.js`'s new-chat click delegation and `ccb:urlchange` handler both run in the **capture phase**, i.e. before the site reacts to the same click. Never decide anything from a one-shot DOM read there — the previous conversation is still fully in the DOM at that moment (2026-07-21). Anything that needs the post-transition state must poll/wait for it.
- Clicking a page button programmatically from a delegated `document` listener re-enters that same listener. Any such click needs a once-per-attempt flag, or it recurses until the stack overflows.
- `verifyPermission()` in `fs-handles.js` must be called from a user-gesture handler — `requestPermission()` on a `FileSystemDirectoryHandle` throws otherwise.
- Code-project file content is never read back onto `project.documents[].content` — always fetch it via `docHandler.getOrExtractContent`/the `codeContent_<id>` key, or a rescan will silently re-duplicate storage.
- This repo lives under a OneDrive-synced folder with a Hebrew path segment; if OneDrive's cloud file provider isn't running, some files can dehydrate to cloud-only placeholders that `git diff` / file reads fail against with `mmap failed: Invalid argument` or "cloud file provider is not running" — start OneDrive if that happens.
- No bundler/build step — all files are loaded directly as MV3 content scripts in the exact order listed in `manifest.json`; adding a new module means adding it to both the manifest array and `content.js#initModules`.
